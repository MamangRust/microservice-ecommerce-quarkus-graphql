quarkus.application.name=merchant-detail-service

quarkus.grpc.server.port=9022
quarkus.grpc.server.host=0.0.0.0

quarkus.http.port=8087
quarkus.http.host=0.0.0.0

# Use %prod. prefix so these only apply in production; test/dev profiles
# will fall through to devservices (testcontainers PostgreSQL) instead.
%prod.quarkus.datasource.db-kind=postgresql
%prod.quarkus.datasource.username=${DB_USER:DRAGON}
%prod.quarkus.datasource.password=${DB_PASS:DRAGON}
%prod.quarkus.datasource.reactive.url=postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:PAYMENT_GATEWAY}
quarkus.datasource.jdbc.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:PAYMENT_GATEWAY}

quarkus.arc.remove-unused-beans=true
quarkus.grpc.server.use-separate-server=true

quarkus.index-dependency.common.group-id=com.sanedge
quarkus.index-dependency.common.artifact-id=common

quarkus.hibernate-orm.schema-management.strategy=update
quarkus.hibernate-orm.database.default-schema=ecommerce_merchant
quarkus.hibernate-orm.log.sql=false

quarkus.log.level=INFO
quarkus.log.category."com.sanedge".level=DEBUG

# Test: no schema management, drop-and-create in public
%test.quarkus.hibernate-orm.schema-management.strategy=none
%test.quarkus.hibernate-orm.database.generation=drop-and-create
%test.quarkus.hibernate-orm.database.default-schema=public

# gRPC Clients
quarkus.grpc.clients.merchant.host=${MERCHANT_HOST:localhost}
quarkus.grpc.clients.merchant.port=${MERCHANT_GRPC_PORT:9005}


# Redis standalone config
quarkus.redis.hosts=${QUARKUS_REDIS_HOSTS:redis://:dragon_knight@localhost:6379}
